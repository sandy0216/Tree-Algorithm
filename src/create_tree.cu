#include <cstdio>
#include <cstring>
#include "../inc/def_node.h"

void createtree(NODE *head, double *x, double *y, double *mass, const double boxsize)
{
	//head->center[0] = boxsize/2;
	//head->center[1] = boxsize/2;
}
